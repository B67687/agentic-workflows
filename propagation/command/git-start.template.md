---
description: Probe git branch, upstream, and worktree state before editing
---

Run:
`bash ./git-session-start.sh`

Then respond compactly with:
- current branch state
- whether anything looks risky
- whether the task should stay here or move to a worktree
- whether it is safe to begin edits now
